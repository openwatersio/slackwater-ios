{
  "name": "slackwater-ios-tools",
  "private": true,
  "type": "module",
  "dependencies": {
    "@sailingnaturali/station-corrections": "^2.9.2"
  },
  "devDependencies": {
    "@neaps/tide-database": "^0.9.20260801",
    "@neaps/tide-predictor": "^0.10.0",
    "tz-lookup": "^6.1.25"
  },
  "scripts": {
    "build:data": "node gen-chs-stations.mjs && node gen-tides.mjs && node gen-noaa-currents.mjs && node gen-chs-gates.mjs",
    "test": "npm run build:data && node --test"
  }
}
