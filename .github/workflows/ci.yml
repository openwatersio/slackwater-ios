name: CI

# ADVISORY, not blocking. openwatersio is a free org and this repo is private,
# so GitHub offers neither branch protection nor rulesets here — nothing can
# reject a push to main server-side. CI reports; the policy it backs is in
# CONTRIBUTING.md and holds by agreement. If the org ever moves to Team, turn
# these two jobs into required checks and the policy stops being voluntary.

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Gates the Studio lane below. There is ONE self-hosted macOS runner, shared
  # with the scheduled jobs and with everyone's local ./scripts/test.sh, and the
  # lane is ~25 minutes — so a docs-only change that books it puts every other
  # session in a queue behind it. Measured: PRs whose entire diff was CLAUDE.md,
  # or two markdown files, each took a full run.
  #
  # This is a JOB-level gate, not `on: paths-ignore`, for two reasons: the free
  # Ubuntu lane below should still run, and a workflow that never starts leaves
  # a required check pending forever, whereas a job skipped by `if:` satisfies
  # it. (CI is advisory today — see the header — but this keeps the door open.)
  #
  # Fail-safe by construction: it skips only when EVERY changed path is docs or
  # a top-level .md. A new directory nobody thought about runs the suite.
  changes:
    name: What changed
    runs-on: ubuntu-latest
    outputs:
      app: ${{ steps.filter.outputs.app }}
    steps:
      # No checkout on purpose. `git diff` would need the history, and a full
      # clone of this repo is 149 MB of committed pmtiles — a minute of runner
      # time on every PR to answer a question the API answers in a second.
      - id: filter
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          EVENT: ${{ github.event_name }}
          PR: ${{ github.event.number }}
          BEFORE: ${{ github.event.before }}
          SHA: ${{ github.sha }}
        run: |
          set -uo pipefail
          run_it() { echo "$1"; echo "app=true" >> "$GITHUB_OUTPUT"; exit 0; }

          if [ "$EVENT" = "pull_request" ]; then
            # --paginate: up to 3000 files, far past anything real here.
            FILES=$(gh api --paginate "repos/$REPO/pulls/$PR/files" --jq '.[].filename') \
              || run_it "could not list PR files — running the app lane"
          else
            case "$BEFORE" in
              ''|0000000000000000000000000000000000000000)
                run_it "no usable base ref — running the app lane" ;;
            esac
            FILES=$(gh api "repos/$REPO/compare/$BEFORE...$SHA" --jq '.files[].filename') \
              || run_it "could not compare $BEFORE...$SHA — running the app lane"
            # The compare endpoint truncates at 300 files. A diff that large is
            # not docs-only, but do not rely on guessing what got cut.
            [ "$(echo "$FILES" | wc -l)" -ge 300 ] && run_it "diff truncated at 300 files — running the app lane"
          fi

          [ -z "$FILES" ] && run_it "no files reported — running the app lane"
          echo "changed:"; echo "$FILES" | sed 's/^/  /'

          OTHER=$(echo "$FILES" | grep -vE '^docs/|^[^/]*\.md$')
          if [ -n "$OTHER" ]; then
            echo "app=true" >> "$GITHUB_OUTPUT"
            echo "-> app lane RUNS (non-docs paths present)"
          else
            echo "app=false" >> "$GITHUB_OUTPUT"
            echo "-> app lane SKIPPED (every path is docs/ or a top-level .md)"
          fi

  data:
    name: Data generators
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: tools/package-lock.json

      - run: npm ci
        working-directory: tools

      # gen-chs-stations.mjs is the only generator that needs the network (the
      # DFO IWLS API), so it stays out of CI and its artefact is trusted as
      # committed. The other two read local files only, which means CI can
      # regenerate them and prove the committed bundles still match — a
      # generator edited without a rerun fails here instead of shipping stale
      # data to the App Store.
      - name: Regenerate the offline bundles
        working-directory: tools
        run: node gen-tides.mjs && node gen-noaa-currents.mjs

      - name: Committed bundles match their generators
        run: git diff --exit-code Slackwater/Resources/stations.json Slackwater/Resources/currents.json

      - name: Bundle invariants
        working-directory: tools
        run: node --test

  app:
    name: App tests
    needs: changes
    if: needs.changes.outputs.app == 'true'
    # Self-hosted on the Mac Studio. GitHub-hosted macOS bills at 10x on a
    # private repo, and the fast plan across both simulators is ~25 min of wall
    # clock — about 250 billable minutes, which would exhaust the monthly
    # allowance in roughly eight runs. The Studio is already the always-on
    # scheduled-job host and keeps a warm SPM cache. See CONTRIBUTING.md.
    runs-on: [self-hosted, macOS]
    steps:
      - uses: actions/checkout@v7

      # The runner is the Mac we develop on, so CI gets its own pair of
      # simulator devices — same device types, different instances. Sharing a
      # device with a local `xcodebuild` run makes the two SIGKILL each other's
      # test runner, which reads as a real test failure and is not one.
      # Created on first run; kept afterwards.
      - name: CI-only simulators
        run: |
          xcrun simctl list devices | grep -q 'SlackwaterCI-iPhone' \
            || xcrun simctl create 'SlackwaterCI-iPhone' 'iPhone 17'
          xcrun simctl list devices | grep -q 'SlackwaterCI-iPad' \
            || xcrun simctl create 'SlackwaterCI-iPad' 'iPad Pro 11-inch (M5)'

      # Runs xcodegen, then the fast suite on both reference simulators — the
      # live-IWLS UI tests skip themselves unless SLACKWATER_FULL is set
      # (scripts/test.sh --full), and that full run stays a pre-upload step
      # run by hand.
      - run: ./scripts/test.sh
        env:
          SLACKWATER_SIMS: SlackwaterCI-iPhone,SlackwaterCI-iPad
